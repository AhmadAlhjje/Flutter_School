# Student app configuration (bundled into the app — public values only, never secrets).

# Backend API address.
# The server (VPS): http://163.245.221.210:6003 — release builds allow plain http only to this
# address (a later https:// address with a domain needs no other change).
# Local development instead: http://localhost:4000 — every debug build then runs `adb reverse`,
# so a phone connected by USB reaches the backend running on this laptop.
API_BASE_URL=http://163.245.221.210:6003
